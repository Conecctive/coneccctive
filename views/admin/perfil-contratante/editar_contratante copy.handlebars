 <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container">
                <a class="navbar-brand" href="#"><img src="/img/svgs/logo-horizontal.svg" alt="Logo da empresa"></a>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ml-auto main-items text-center">
                        <li>
                            <form class="form-inline d-lg-block">
                                <input class="form-control" type="text" placeholder="Pesquisar" aria-label="Pesquisar">
                                <button class="btn btn-outline-success" type="submit">Pesquisar</button>
                            </form>
                        </li>
                    </ul>
                </div>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <div class="dropdown">
                            <!-- Use um ícone de usuário como bolinha de perfil -->
                            <i class="fas fa-user fa-2x dropdown-toggle" id="foto-perfil" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"></i>
                            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="foto-perfil">
                                <a class="dropdown-item" href="#">Editar Perfil</a>
                                <a class="dropdown-item" href="#">Alternar Perfil</a>
                                <a class="dropdown-item" href="#">Sair</a>
                            </div>
                        </div>
                    </li>
                </ul>
              
                
            </div>
        </nav>
    </header>

    <main class="profile">
        <form action="/editar-contratante" method="post" enctype="multipart/form-data">
        <input type="hidden" name="id_contratante" id="id_contratante" value="{{data.id_contratante}}">
            <section class="basic-info">
                 <div class="foto-perfil">
    <input type="file" id="imagemperfil" name="imagemperfil" accept="image/*">
    <img class="profile-picture" id="imagem" src="/img/icones/perfil-de-usuario.png" alt="Foto de Perfil">
    <label for="imagemperfil"><i class="fa-solid fa-camera" style="color: #4c00a8;"></i></label>
</div>
                </div>
                <label for="cidade">Cidade:</label>
                <input type="text" id="cidade" name="cidade" placeholder="Digite sua cidade">
        
                <label for="estado">Estado:</label>
                <select id="estado" name="estado">
                    <option value="">Selecione seu estado</option>
                    <option value="AC">Acre (AC)</option>
                    <option value="AL">Alagoas (AL)</option>
                    <option value="AP">Amapá (AP)</option>
                    <option value="AM">Amazonas (AM)</option>
                    <option value="BA">Bahia (BA)</option>
                    <option value="CE">Ceará (CE)</option>
                    <option value="DF">Distrito Federal (DF)</option>
                    <option value="ES">Espírito Santo (ES)</option>
                    <option value="GO">Goiás (GO)</option>
                    <option value="MA">Maranhão (MA)</option>
                    <option value="MT">Mato Grosso (MT)</option>
                    <option value="MS">Mato Grosso do Sul (MS)</option>
                    <option value="MG">Minas Gerais (MG)</option>
                    <option value="PA">Pará (PA)</option>
                    <option value="PB">Paraíba (PB)</option>
                    <option value="PR">Paraná (PR)</option>
                    <option value="PE">Pernambuco (PE)</option>
                    <option value="PI">Piauí (PI)</option>
                    <option value="RJ">Rio de Janeiro (RJ)</option>
                    <option value="RN">Rio Grande do Norte (RN)</option>
                    <option value="RS">Rio Grande do Sul (RS)</option>
                    <option value="RO">Rondônia (RO)</option>
                    <option value="RR">Roraima (RR)</option>
                    <option value="SC">Santa Catarina (SC)</option>
                    <option value="SP">São Paulo (SP)</option>
                    <option value="SE">Sergipe (SE)</option>
                    <option value="TO">Tocantins (TO)</option>
                </select>     
            </section>
            <section class="contact secao">
                <h2><i class="fas fa-envelope"></i>Contato</h2>
                <p><i class="fas fa-envelope"></i><input type="email" name="email" placeholder="usuario@email.com" id=""></p>
                <p><i class="fas fa-phone"></i><input type="tel" name="telefone" id="telefone" placeholder="(xx) xxxxx-xxxx"></p>
            </section>
            <section class="about-me secao">
                <h2><i class="fas fa-user"></i>Sobre Mim</h2>
                <input type="text" name="sobremim" id="sobremim" placeholder="Uma breve descrição sobre o usuário...">
            </section>
            <section class="connections secao">
                <h2><i class="fas fa-link"></i>Conexões</h2>
                <ul id="connectionsList">
                    <!-- Conteúdo de conexões será adicionado aqui -->
                </ul>
                <input type="button" id="addConnection" value="Adicionar conexão" >
            </section>
            <div class="botao-form">
                <input type="submit" value="Salvar">
                <input type="button" value="Cancelar" onclick="voltarParaTelaAnterior()">
            </div>
        </form>
    </main>
    <footer class="footer">

        <div class="container text-center">
          <div class="termos">
            <p>Privacidade | Termos e Condições | Insenção de Responsabilidade</p>
          </div>
          <a href="#" class="social-icon"><i class="fab fa-instagram"></i></a>
          <h4>Copyright 2023 - Conecctive.com</h4>
        </div>
      </footer>
      <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
      <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
      <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
      <script src="https://kit.fontawesome.com/c0f408d1cc.js" crossorigin="anonymous"></script>
      <script src="navbar.js"></script>
      <script>
        const navbarToggler = document.querySelector(".navbar-toggler");
        const loginCadastroItems = document.querySelector(".navbar-nav.ml-auto");
    
        navbarToggler.addEventListener("click", function() {
          loginCadastroItems.classList.toggle("show");
        });
        /*Colocando a imagem no perfil*/
        const profilePictureInput = document.getElementById('profile-picture-input');
        const profilePicture = document.querySelector('.profile-picture');

        profilePictureInput.addEventListener('change', () => {
            const selectedFile = profilePictureInput.files[0];
            if (selectedFile) {
                profilePicture.src = URL.createObjectURL(selectedFile);
            }
        });

        /*Voltar para tela anterior quando clicar no botao cancelar*/
        function voltarParaTelaAnterior() {
            window.history.back();
        }

      </script>
      <script>
        // Função para adicionar uma nova conexão
function adicionarConexao() {
    const listaConexoes = document.getElementById("connectionsList");

    // Criar um novo item de lista (li)
    const novaConexao = document.createElement("li");
    
    // Conteúdo do item de lista
    novaConexao.innerHTML = `
        <input type="text" name="" id="" class="inputs-conexoes">
        <span>
            <button type="button" class="btn btn-danger remover-btn">
                <i class="fas fa-trash"></i> Remover
            </button>
        </span>
    `;

    // Adicionar o novo item de lista à lista de conexões
    listaConexoes.appendChild(novaConexao);

    // Associar a função de remover conexão ao botão "Remover" dentro do novo item
    const botaoRemover = novaConexao.querySelector(".remover-btn");
    botaoRemover.addEventListener("click", () => {
        removerConexao(novaConexao);
    });
}

// Função para remover uma conexão
function removerConexao(itemConexao) {
    const listaConexoes = document.getElementById("connectionsList");
    listaConexoes.removeChild(itemConexao);
}

// Associar a função de adicionar conexão ao botão "Adicionar conexão"
const botaoAdicionar = document.getElementById("addConnection");
botaoAdicionar.addEventListener("click", adicionarConexao);

      </script>